import { useSignal } from "@preact/signals";
import Counter from "../islands/Counter.tsx";

export default function Home() {
  
  return (
    <div>
     <a href="/users">Ver los Usuarios de la Api</a>
    </div>
  );
}
